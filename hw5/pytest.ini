[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = module
testpaths = tests
markers =
    integration: marks tests as integration (real DB, Redis, etc.). Run with: pytest -m integration
